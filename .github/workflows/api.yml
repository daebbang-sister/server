name: API Server CI/CD

on:
  push:
    branches: [ "main" ]
    paths:
      - 'daebbang-api/**'
      - 'daebbang-core/**'
      - 'daebbang-common/**'
      - '.github/workflows/api.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

        # Java 17 Setting (Gradle Cache)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

        # Gradle Build (API)
      - name: Build API Module
        run: |
          chmod +x ./gradlew
          ./gradlew :daebbang-api:bootJar

        # Jar file Extract
      - name: Extract Jar Layers
        run: |
          cp daebbang-api/build/libs/*.jar daebbang-api/app.jar
          cd daebbang-api
          java -Djarmode=tools -jar app.jar extract --layers --launcher

        # Docker Buildx Settings
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

        # Docker Login
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

        # Docker Registry Push
      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ./daebbang-api
          file: ./daebbang-api/Dockerfile-prod
          push: 'true'
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/daebbang-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

        # Docker Scout
      - name: Docker Scout
        uses: docker/scout-action@v1
        with:
          command: quickview,cves
          image: ${{ secrets.DOCKERHUB_USERNAME }}/daebbang-server:latest
          exit-code: true
          only-severities: critical, high